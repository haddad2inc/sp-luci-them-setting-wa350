# تقرير تدقيق شامل — مجلد `files` في مستودع sp-luci-them-setting-wa350

**التاريخ:** 2026-09-06 · **النطاق:** 25 ملفًا، ~7,150 سطرًا داخل `files/` فقط (حزمة الأوبن ورت مؤجلة).
**المرجع:** معايير مجتمع OpenWrt/LuCI الرسمية (قورنت مع `luci-theme-bootstrap` الرسمي على فرع `openwrt-24.10`).

---

## تصنيف النتائج

- 🔴 **حرج:** أمن / مخالفات جوهرية لمعايير المجتمع — يجب إصلاحها أولًا.
- 🟠 **متوسط:** انحراف عن اصطلاحات LuCI أو هشاشة صيانة.
- 🟡 **منخفض:** تجميل، تسمية، أخطاء صغيرة.

---

## أولاً: النتائج الحرجة 🔴

### H1. حذف كلمة مرور الروت مع بقاء SSH مفتوحًا بكلمة مرور
**الملف:** `files/etc/uci-defaults/99-asu-defaults-general` (سطر 200-203)
```
passwd -d root
uci set dropbear.@dropbear[0].PasswordAuth='1'
uci set dropbear.@dropbear[0].RootPasswordAuth='1'
```
الجهاز يُشحن بدون أي كلمة مرور، مع تفعيل مصادقة كلمة المرور لـ root عبر SSH. أي شخص على الشبكة يملك الجهاز بالكامل.
**المعيار:** أجهزة المجتمع إما تُشحن بكلمة مرور أو بمفاتيح SSH فقط، وLuCI الرسمي يعرض تحذيرًا عند غياب كلمة المرور.
**الإصلاح المقترح:** عدم حذف كلمة المرور، أو على الأقل تعطيل PasswordAuth والاكتفاء بالمفاتيح، وإضافة تحذير "لا توجد كلمة مرور" في الثيم (انظر T3).

### H2. تعطيل الجدار الناري بالكامل وإخفاء ملفه
**الملف:** `99-asu-defaults-general`
```
/etc/init.d/firewall disable + stop
mv /etc/config/firewall /etc/config/firewall.unused
```
المجتمع يعتبر الشحن بدون جدار ناري (nftables) خطًا أحمر حتى لأجهزة الجسر/الـ AP. كما أن إخفاء الملف يمنع أي إضافة مستقبلية من العمل.
**الإصلاح المقترح:** الإبقاء على الجدار الناري بقواعد دنّى تسمح الإدارة فقط، أو توثيق السبب وإبقاء الملف.

### H3. تعطيل زر الريسيت نهائيًا (فقدان Failsafe)
**الملفات:**
- `99-asu-defaults-general` سطر 12: `uci delete system.@button[0]`
- `files/etc/init.d/wizard` سطر ~250: عند `disable_reset_button=1` يكتب سكربت فارغ فوق `/etc/rc.button/reset`، وعند إلغائه **يحذف الملف بالكامل** بدل استعادة السلوك الافتراضي.
**المعيار:** زر الريسيت = طريق النجاة الوحيد للمستخدم (failsafe/factory reset). تعطيله بهذه الطريقة غير مقبول مجتمعيًا.
**الإصلاح المقترح:** إن لزم تعطيله، اجعله عبر طبقة إضافية تحفظ الأصل وتعيده، وليس حذف ملف الـ base-files.

### H4. كلمة مرور الروت تُخزَّن نصًا صريحًا في UCI
**الملفات:** `files/etc/init.d/wizard` (سطر 67-89) + `files/www/luci-static/resources/view/wizard.js`
المعالج يكتب `wizard.default.root_password` داخل `/etc/config/wizard` (صلاحيات 0644 يقرؤها أي مستخدم) ثم يمررها إلى `passwd` عبر echo.
**المعيار:** لا تُخزَّن كلمات المرور في ملفات الإعدادات إطلاقًا. طريقة LuCI الرسمية: استدعاء مباشر (مثل `luci.setPassword` عبر rpc) دون أي تخزين وسيط.
**الإصلاح المقترح:** تغيير كلمة المرور في خطوة الحفظ نفسها عبر ubus بدل تخزينها.

### H5. شبكات مفتوحة بدون تشفير كإعداد افتراضي
**الملفات:** `etc/config/wizard`، `99-asu-defaults-general`، ومنطق `init.d/wizard`
`encryption='none'` هو الافتراضي للـ AP والميش والعميل (`mesh_encryption 'none'`, `client_encryption 'none'`)، وشبكتا البث الافتراضيتان `COMFAST-WA350` مفتوحتان.
**المعيار:** الافتراضي المجتمعي هو WPA2-PSK على الأقل.
**ملاحظة:** إن كان هذا قرارًا منتجياً مقصودًا (تهيئة أولية ثم ضبط)، فيجب توثيقه بوضوح.

---

## ثانيًا: انحرافات عن معايير ثيمات LuCI 🟠

### T1. اعتماد خارجي على خطوط Google Fonts
**الملفان:** `header.ut` سطر 10-12 و`sysauth.ut` سطر 10-12
```
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Inter...&family=Noto+Sans+Arabic...">
```
**المشاكل:**
- الراوتر غالبًا بلا إنترنت → الثيم يفقد خطوطه ويتأخر التحميل (مهلة انتظار الشبكة).
- تسريب خصوصية: متصفح المدير يتصل بخوادم جوجل من داخل شبكة الإدارة.
**المعيار:** الثيمات الرسمية لا تحمل أي مورد خارجي؛ الخطوط إما محلية أو خطوط نظام (`font-family` مع بدائل).

### T2. سكريبت الترجمات غير محمّل → الواجهة لا تُترجم
`header.ut` لا يتضمن السطر القياسي الموجود في الثيم الرسمي:
```
<script src="{{ dispatcher.build_url('admin/translations', dispatcher.lang) }}"></script>
```
النتيجة: كل نصوص واجهات LuCI المبنية بـ JS (عناوين القوائم، حقول CBI، Save & Apply…) تظهر بالإنجليزية حتى لو اختار المستخدم العربية.

### T3. غياب التحذيرات القياسية في رأس الصفحة
الثيم الرسمي يعرض في `header.ut`:
1. تحذير "لا توجد كلمة مرور!" (حاسم هنا بسبب H1).
2. تحذير وضع initramfs.
3. تحذير `<noscript>`.
الثيم الحالي لا يعرض أيًا منها.

### T4. غياب إسناد "Powered by LuCI / OpenWrt"
`footer.ut` يعرض توقيعًا شخصيًا فقط (رابط فيسبوك + رقم هاتف 780405427).
**المعيار:** تذييل الثيمات الرسمية يعرض إسنادًا لـ LuCI (`version.luciname/luciversion`) وتوزيعة OpenWrt (`version.distname/distversion/disturl`). الأرقام الشخصية والروابط الاجتماعية لا تُقبل في ثيمات المجتمع.

### T5. اسم المستخدم مكتوب يدويًا "root"
**الملف:** `header.ut` سطر 55: `<span>root</span>` مع صورة رمزية حرف "R".
يجب جلب اسم المستخدم الفعلي من جلسة الدخول.

### T6. قيم احتياطية خاصة بجهاز واحد داخل الثيم
**الملف:** `footer.ut`: `'CF-WA350'`, `'QCA9563'`, `'128MB'`, `'OpenWrt 24.10.8'` كقيم بديلة عند غياب `boardinfo`.
**المعيار:** الثيم يجب أن يكون مستقلًا عن العتاد؛ عند الغياب تُعرض قيم عامة ("Unknown") أو يُخفى السطر.

### T7. المتغير `pkg_version` غير موجود في نطاق قوالب ucode
يُستخدم في 6 مواضع (`?v={{ pkg_version }}`). الثيم الرسمي يستخدم كائن `version.*` (مثل `version.luciversion`). المتغير الحالي غير معرّف → يُطبع فارغًا، أي أن كسر الكاش لا يعمل أصلًا.
**(يلزم تحقق سريع من مصدر الصفحة على الجهاز).**

### T8. منتقي خاطئ لرسائل التنبيه
**الملف:** `enan-dashboard.css`: `.enan-flash-container .alert` بينما فئات LuCI الفعلية هي `.alert-message` (+ الأنواع `warning/error/success`). التنسيق لا يُطبَّق.

### T9. منطق Status الاحتياطي يعتمد على نصوص إنجليزية
**الملف:** `enan-dashboard.js`: البحث عن أقسام بعنواني `"Memory"` و`"Storage"` واستبدال `"Hide|Show"`.
ينكسر تمامًا مع واجهة عربية أو أي لغة أخرى. المعيار: بناء البيانات من نفس الـ RPC الذي تستخدمه صفحة الحالة بدل مطابقة النصوص.

### T10. قائمة Fallback ثابتة قد تشير لصفحات غير موجودة
**الملف:** `enan-navigation.js`: عند فشل تحميل القائمة يبني قائمة يدوية تتضمن `network/firewall` — لكن ملف `99-asu-defaults-general` نفسه يعطّل الجدار الناري ويخفي ملفه! أي أن الثيم يعرض رابطًا لصفحة معطلة في نفس الفيرموير.

### T11. لا يوجد دعم للاتجاه العربي RTL
الواجهة تُسوَّق بالعربية (نصوص المعالج والقوائم عربية) لكن التخطيط يعتمد `margin-left` ثابتًا ولا يوجد أي معالجة لـ `dir="rtl"`؛ ستنعكس الواجهة بشكل مكسور عند اختيار اللغة العربية.

### T12. فرض صفحة البداية من الثيم
`30_luci-theme-enan-dashboard` يضبط `luci.main.index` على لوحة الثيم. اصطلاحيًا هذا قرار تطبيق وليس قرار ثيم؛ إن بقي فيجب ضمان عدم انكسار الدخول عند إزالة الثيم لاحقًا.

### T13. الاعتماد الكثيف على `!important`
مئات القواعد تتجاوز أنماط LuCI بـ `!important`. يعمل، لكنه يصعّب الصيانة والتحديث مع كل إصدار LuCI. (مقبول لدى ثيمات طرف ثالث مثل Argon، لكنه يُسجَّل كنقطة صيانة.)

---

## ثالثًا: ملفات النظام (خارج الثيم) 🟠/🟡

### S1. نسخ كاملة من ملفات الـ base-files تُظلّل الأصلية 🔴 (مفهومياً)
- `etc/board.d/01_leds` (~580 سطرًا) و`02_network` (~900 سطر): نسخ كاملة لملفات منصة ath79 الرسمية، والإضافة الفعلية هي سطران فقط لـ `comfast,cf-wa350`.
- `etc/hotplug.d/firmware/11-ath10k-caldata` (~270 سطرًا): نسخة كاملة، مع أن دعم cf-wa350 موجود الآن في المنبع.
**المشكلة:** أي ملفات داخل `files/` تظلّل ملفات الصورة الأصلية، فتُفقد كل إصلاحات المنبع المستقبلية لهذه المنصات، وتصبح قديمة مع كل إصدار.
**المعيار:** دعم جهاز جديد يكون عبر باتش للمنصة (target) أو سكربت board.d صغير منفصل باسم فريد.

### S2. ترقيع ملفات LuCI المثبتة بأوامر `sed` عند أول إقلاع
**الملف:** `99-asu-defaults-general`: يعدّل `10_system.js` و`/etc/openwrt_release` و`/etc/os-release` وحتى قالب ثيم `bootstrap` بإلحاق رابط فيسبوك.
**المشكلة:** هش — يُكسر بأي تحديث للوسي، ويطمس ملفات النظام.
**المعيار:** العلامات التجارية تُبنى وقت الترجمة (build) أو عبر ملفات بديلة كاملة، وليس `sed` على ملفات حية.

### S3. ملف فارغ ومجلد باسم خاطئ
`files/etc/wizerd/public.key`: ملف حجمه 0 بايت، والمجلد "wizerd" (خطأ إملائي، الصحيح "wizard"). يبدو أثرًا لنظام تحديث موقّع لم يكتمل. يُحذف أو يُستكمل.

### S4. ملاحظات على `etc/init.d/wizard`
- الدالة `rename_radio()` معرَّفة **داخل** كتلة شرطية داخل `_apply_direct` (سطر ~107) وبمسافات بدل التبويب — مخالف لأسلوب سكربتات بروكد.
- إعادة تسمية الراديوات من `radio0/radio1` إلى `2G/5G`: اصطلاح المجتمع هو `radioN` الثابت؛ بعض الأدوات والسكربتات تفترض هذا النمط.
- `isolate='1'` (عزل العملاء) مفروض افتراضيًا على كل شبكات الـ AP.
- Watchcat مبرمج على `8.8.8.8` ثابتًا.
- `sleep 3` داخل سكربت procd (الأفضل محفزات واجهات).
- `uci -q commit dhcp` رغم أن المعالج لا يعدّل dhcp إطلاقًا.

### S5. نصوص المصدر عربية بدل الإنجليزية (i18n)
**الملفات:** `wizard.js`، `luci-app-wizard.json` (`"title": "البرمجة السريعة"`).
**المعيار:** نصوص المصدر في الكود تكون إنجليزية، والترجمات تُقدَّم عبر ملفات `.po` (luci-i18n). استخدام `_()` حول نص عربي أصلي يجعل الترجمة مستحيلة لاحقًا.

### S6. `01_uhttpd_ubus`
يصلح مسار `ubus_socket` ويضبط `ubus_prefix` — يبدو التفافًا على مشكلة بناء مخصص. في البناء القياسي غير ضروري؛ إن بقي فلِحالة محددة فقط.

### S7. أخطاء صغيرة متفرقة
- `99-asu-defaults-general`: `uci set system.@system.cronloglevel='5'` صيغة غير صالحة دائمًا (يسقط للسطر الاحتياطي)؛ `DISTRIB_DESCRIPTION='Haddad-Inc @'` فيها `@` يبدو بقايا غير مكتملة؛ تعليق "لـ OpenWrt 25" مع `htmode='HT20'` على راديو 5GHz (يقيّد سرعة AC بشدة) و`country='US'` ثابت وقنوات ثابتة — قرارات منتجية لكنها تسجَّل كمخالفة لأفضل الممارسات التنظيمية والأدائية.
- `dashboard-widgets.js`: التعبير `/\\.([0-9]+)$/` يقصد به استبعاد أسماء VLAN مثل `eth0.1` لكنه يطابق عمليًا شرطة مائلة معكوسة — الفلتر لا يعمل.
- `98-llpd-setting`: اسم الملف نفسه خطأ إملائي (الصحيح `98-lldpd-setting`).
- صلاحيات التنفيذ محفوظة جيدًا على كل السكربتات ✅.

---

## خلاصة بالأرقام

| الفئة | العدد |
|---|---|
| 🔴 حرج (أمن/مجتمع) | 5 |
| 🟠 متوسط (معايير/صيانة) | 16 |
| 🟡 منخفض | 6 |

**الحكم العام:** الثيم نفسه (CSS/JS/قوالب) مصنوع بإتقان تقني واضح ومتوافق وظيفيًا مع LuCI الحديث، لكن الفجوة الحقيقية مع معايير المجتمع تتركز في: (1) قرارات الأمان في الفيرموير، (2) تبعيات الإنترنت في ثيم يُفترض أن يعمل أوفلاين، (3) تظلّل ملفات المنصة الكاملة، (4) نقص الترجمة والإسناد وRTL.

---

## ترتيب الإصلاح المقترح (خطوة بخطوة)

1. H1 + T3 — كلمة المرور وتحذيرها.
2. H2 — الجدار الناري.
3. H3 — زر الريسيت.
4. H4 — نقل تغيير كلمة المرور من UCI.
5. T1 + T2 — الخطوط المحلية + سكريبت الترجمات.
6. T4 + T5 + T6 — التذييل والإسناد والمستخدم الفعلي.
7. T7..T11 — إصلاحات الثيم المتوسطة.
8. S1..S3 — ملفات النظام.
9. الحزمة (openwrt-integration) بعد الانتهاء من كل ما سبق.
